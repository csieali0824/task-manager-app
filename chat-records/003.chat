Chat 003 — 前端 UI 全面重新設計（Tailwind CSS）、操作限制邏輯、專案結構調整
工具：Claude Code（Claude, Anthropic）
日期：2026-08-20 ～ 2026-08-21

AI 使用目的：
- 新增前端操作限制：未勾選（未完成）的任務按下「編輯」或「刪除」時，跳出提醒
  視窗並阻止操作；只有已勾選（完成）的任務才能編輯或刪除。
- 引入 Tailwind CSS v4（官方 @tailwindcss/vite plugin），參考國際頂尖產品
  （Linear / Vercel / Stripe）的設計語言全面重新設計 UI。
- 將 chat-records/001.chat 改寫為繁體中文（專有名詞如 entity / repository /
  service / controller / DTO 保留英文）。
- 調整 Maven 模組名稱與專案結構，讓 IDE 顯示的模組名與專案目錄一致。

對話過程摘要：
1. 使用者指出任務不論勾選與否都能編輯／刪除，希望改為「只有已勾選的任務才能
   操作，未勾選時跳出提醒視窗」。Assistant 在 App.vue 的 onEdit / onRemove 加上
   task.completed 檢查與 window.alert 提醒。
2. 使用者要求改用 Tailwind CSS 並改善畫面美感。Assistant：
   - 安裝 tailwindcss + @tailwindcss/vite，設定 vite.config.ts 與 main.css，
     移除舊的 scaffold 樣式（base.css）。
   - 新增可重用的 BaseButton.vue 元件（primary / secondary / danger / ghost
     四種樣式）。
   - 重寫 App.vue / TaskForm.vue / TaskList.vue / TaskItem.vue 的 template 與
     樣式（功能邏輯不變）。
3. 使用者進一步要求參考國外頂尖設計公司的水準。Assistant 二次打磨：
   - index.html 引入 Inter + Noto Sans TC 字體（Google Fonts）。
   - 頁面頂部加入靛藍 radial 光暈背景、毛玻璃卡片（backdrop blur + 髮絲級
     邊框）、漸層進度條與完成度統計、漸層打勾 logo。
   - 微互動：客製圓形 checkbox（勾選時漸層填色 + 打勾縮放動畫）、桌面版
     hover 才浮現的編輯／刪除按鈕、TransitionGroup 列表進出場動畫、按鈕
     按壓縮放回饋。
   - 深色／淺色模式皆完整支援；介面文字改為繁體中文。
   - 每次改動後皆以 npm run build（含 type-check）驗證通過。
4. 使用者要求將 001.chat 改寫為繁體中文（保留英文專有名詞），Assistant 完成
   全文改寫，內容事實不增不減。
5. 使用者希望 IDE 中顯示的 Maven 模組名從 task-manager-backend 改為
   task-manager-app。Assistant 先將 pom.xml 的 artifactId 與 name 改為
   task-manager-app；因 IntelliJ 顯示的仍是「資料夾名 [模組名]」格式，使用者
   決定直接把 Maven 專案掛到專案根目錄：
   - 以 git mv 將 backend/pom.xml、backend/src 搬到專案根目錄（保留 git
     rename 歷史），移除 backend/ 資料夾。
   - 同步更新 .gitignore（backend/target/ → target/）、.idea/misc.xml（Maven
     專案路徑）、.idea/compiler.xml（模組名）、README.md（啟動指令不再需要
     cd backend、entity 路徑、測試指令），以及 application.yml 的
     spring.application.name。
   - 在根目錄執行 mvn verify 確認測試全數通過。

異動檔案：
  pom.xml                                   （原 backend/pom.xml；artifactId/name 改名後搬移）
  src/**                                    （原 backend/src/**，整批搬移，內容未改）
  src/main/resources/application.yml        （spring.application.name 改名）
  .gitignore                                （target 路徑更新）
  .idea/misc.xml、.idea/compiler.xml        （Maven 路徑與模組名更新）
  README.md                                 （啟動／測試指令與路徑更新）
  chat-records/001.chat                     （改寫為繁體中文）
  frontend/index.html                       （字體、lang、title）
  frontend/vite.config.ts                   （加入 tailwindcss plugin）
  frontend/package.json、package-lock.json  （新增 tailwindcss、@tailwindcss/vite）
  frontend/src/assets/main.css              （改為 Tailwind 設定與動畫定義）
  frontend/src/assets/base.css              （刪除，未再使用）
  frontend/src/components/BaseButton.vue    （新增）
  frontend/src/components/TaskForm.vue      （重新設計）
  frontend/src/components/TaskItem.vue      （重新設計 + 客製 checkbox）
  frontend/src/components/TaskList.vue      （重新設計 + 列表動畫）
  frontend/src/App.vue                      （重新設計 + 操作限制邏輯）
